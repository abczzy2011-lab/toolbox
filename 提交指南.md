# 🚀 Homebrew Tap 发布指南

> 通过个人 Tap 发布万能工具箱，用户用 `brew install tool-box` 安装

---

## 第 1 步：注册 GitHub 账号

1. 浏览器打开 [github.com](https://github.com)
2. 点 **Sign up**，填邮箱、用户名、密码
3. 按邮件里的链接激活账号

> 记录你的 GitHub 用户名，后面要用

---

## 第 2 步：安装 GitHub CLI

```bash
brew install gh
```

---

## 第 3 步：登录 GitHub CLI

```bash
gh auth login
```

按提示选：
- Git protocol → **HTTPS**
- 浏览器自动打开，点 **Authorize**

验证：
```bash
gh auth status
```

---

## 第 4 步：配置 Git

```bash
git config --global user.name "你的GitHub用户名"
git config --global user.email "你的邮箱"
```

---

## 第 5 步：创建工具仓库

```bash
gh repo create toolbox \
  --public \
  --description "27 free offline web-based utilities" \
  --add-readme \
  --license MIT
```

---

## 第 6 步：把项目推上去

```bash
cd /Users/leozhang/Desktop/万能工具箱

git init
git remote add origin https://github.com/abczzy2011-lab/toolbox.git
git add .
git commit -m "Initial release: v1.0.0"
git push -u origin main
```

---

## 第 7 步：上传 Release ZIP

```bash
# 把中文文件名改英文
cp 万能工具箱-本地版.zip /tmp/toolbox.zip

# 上传到 GitHub Release
gh release create v1.0.0 \
  --title "v1.0.0" \
  --notes "First release with 27 tools" \
  /tmp/toolbox.zip
```

---

## 第 8 步：创建 Tap 仓库

```bash
# 创建 tap 仓库
gh repo create homebrew-tap \
  --public \
  --description "Homebrew tap for ToolBox" \
  --add-readme
```

本地创建 tap：
```bash
brew tap-new abczzy2011-lab/tap
```

---

## 第 9 步：写入公式

编辑 `Formula/tool-box.rb`：

```ruby
# frozen_string_literal: true

class ToolBox < Formula
  desc "Offline web-based toolkit with 27 utilities"
  homepage "https://github.com/abczzy2011-lab/toolbox"
  url "https://github.com/abczzy2011-lab/toolbox/releases/download/v1.0.0/toolbox.zip"
  sha256 "填写实际的sha256"
  license "MIT"

  def install
    libexec.install Dir["*"]
    bin.write_exec_script libexec/"tool-box"
  end
end
```

SHA256 生成：
```bash
sha256sum /tmp/toolbox.zip
```

---

## 第 10 步：推送 Tap 到 GitHub

```bash
cd /opt/homebrew/Library/Taps/abczzy2011-lab/homebrew-tap

git remote add origin https://github.com/abczzy2011-lab/homebrew-tap.git
git add -A
git commit -m "tool-box 1.0.0 (new formula)"
git push -u origin main
```

---

## 第 11 步：验证安装

```bash
brew untap abczzy2011-lab/tap

brew tap abczzy2011-lab/tap
brew install tool-box

tool-box --help
tool-box ip
```

---

## 第 12 步：更新版本

```bash
# 修改工具后重新打包
# 更新 GitHub Release
# 更新公式 URL 和 SHA256

cd /opt/homebrew/Library/Taps/abczzy2011-lab/homebrew-tap

# 编辑公式，改 URL 和 sha256
code Formula/tool-box.rb

# 提交推送
git add -A
git commit -m "tool-box 1.0.5"
git push
```

用户更新：
```bash
brew update && brew upgrade tool-box
```

---

## ✅ 最终效果

用户只需三行：

```bash
brew tap abczzy2011-lab/tap
brew install tool-box
brew services start tool-box
```

或一行：

```bash
brew tap abczzy2011-lab/tap && brew install tool-box && brew services start tool-box
```
