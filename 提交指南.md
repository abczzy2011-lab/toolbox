# 🚀 Homebrew-core 提交指南

> 跟着以下步骤，把你的万能工具箱提交到 Homebrew-core，实现 `brew install toolbox`

---

## 第 1 步：注册 GitHub 账号

1. 浏览器打开 [github.com](https://github.com)
2. 点 **Sign up**，填邮箱（用 `17625100088@163.com`）、用户名、密码
3. 按邮件里的链接激活账号

> **记住abczzy2011-lab**（比如 `LeoZhang`），后面要用

---

## 第 2 步：安装 GitHub CLI

```bash
brew install gh
```

---

## 第 3 步：登录 GitHub CLI

```bash
gh auth login
```

按提示选：
- Git protocol → **HTTPS**
- 浏览器会自动打开，点 **Authorize**
- 完成后终端会自动继续

验证：
```bash
gh auth status
```

---

## 第 4 步：配置 Git

```bash
git config --global user.name "你的GitHub用户名"
git config --global user.email "17625100088@163.com"
```

> 用户名填你第 1 步注册的用户名，比如 `git config --global user.name "LeoZhang"`

---

## 第 5 步：在 GitHub 创建仓库

### 方法 A：用 gh 命令行

```bash
# 创建公开仓库
gh repo create toolbox \
  --public \
  --description "27 free offline web-based utilities" \
  --add-readme \
  --license MIT
```

### 方法 B：网页操作

1. 打开 [github.com/new](https://github.com/new)
2. 仓库名填 `toolbox`
3. 选 **Public**
4. 勾选 **Add a README file** 和 **Add .gitignore**
5. 点 **Create repository**

---

## 第 6 步：把项目推上去

```bash
cd /Users/leozhang/Desktop/万能工具箱

# 初始化 git（如果还没有）
git init

# 配置远程仓库
git remote add origin https://github.com/abczzy2011-lab/toolbox.git

# 添加所有文件
git add .

# 提交
git commit -m "Initial release: v1.0.4"

# 推送
git push -u origin main
```

---

## 第 7 步：打 Release Tag

### 用 gh 命令行

```bash
gh release create v1.0.4 \
  --title "v1.0.4" \
  --notes "First release with 27 tools" \
  万能工具箱-本地版.zip
```

### 或者网页操作

1. 打开 [github.com/abczzy2011-lab/toolbox/releases](https://github.com/abczzy2011-lab/toolbox/releases)
2. 点 **Draft a new release**
3. Tag: `v1.0.4`
4. 上传 `万能工具箱-本地版.zip`
5. 点 **Publish release**

---

## 第 8 步：创建 Tap（你的公式仓库）

```bash
# 创建你的 tap 仓库
gh repo create homebrew-toolbox \
  --public \
  --description "Homebrew tap for toolbox" \
  --add-readme
```

本地创建 tap：
```bash
brew tap-new my-org/toolbox
```

---

## 第 9 步：更新公式中的 URL 和 SHA256

发布后，GitHub Release 的 ZIP URL 会是：
```
https://github.com/abczzy2011-lab/toolbox/releases/download/v1.0.4/万能工具箱-本地版.zip
```

但文件名有中文，建议改名为英文：
```bash
cp 万能工具箱-本地版.zip /tmp/toolbox.zip
```

然后在 GitHub Release 里上传 `toolbox.zip` 而不是中文名。

更新公式中的 URL：
```ruby
url "https://github.com/abczzy2011-lab/toolbox/releases/download/v1.0.4/toolbox.zip"
```

更新 SHA256：
```bash
sha256sum toolbox.zip
# 把结果填到公式里
```

---

## 第 10 步：本地验证公式

```bash
# 安装你的 tap
brew tap my-org/toolbox

# 验证公式
brew install toolbox

# 测试
brew test toolbox

# 审计
brew audit --new --formula toolbox
```

---

## 第 11 步：向 Homebrew-core 提 PR

### 创建本地公式文件

```bash
cd $(brew --repository homebrew/core)
brew create https://github.com/abczzy2011-lab/toolbox/releases/download/v1.0.4/toolbox.zip
```

这会生成 `Formula/t/tool-box.rb`。

### 编辑公式

编辑生成的 `.rb` 文件，确保内容正确。

### 提交 PR

```bash
cd $(brew --repository homebrew/core)

git checkout -b toolbox

git add Formula/t/tool-box.rb
git commit -m "tool-box 1.0.4 (new formula)"

git push origin toolbox

gh pr create --fill
```

在 PR 描述里写：
```
This formula installs ToolBox, an offline web-based toolkit with 27 utilities
including calculator, text tools, color picker, QR generator, etc.

Homepage: https://github.com/abczzy2011-lab/toolbox
License: MIT
```

---

## ⚠️ 注意事项

1. **先在你的 tap 里跑通**，再提 homebrew-core PR
2. **SHA256 必须正确**，否则 CI 会失败
3. **公式命名**：`tool-box.rb`，类名 `ToolBox`
4. **提交消息格式**：`tool-box 1.0.4 (new formula)`
5. **PR 描述**要写清楚是什么软件
6. **等待 CI 全部通过**（macOS + Linux 矩阵）